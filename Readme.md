This creates every flutter code sample

see \$flutter create -h

flutter create --sample=sample_id

If you want to recreate them yourself, delete every folder and the samples file and do:

```
$ dart createSamples.dart
```
